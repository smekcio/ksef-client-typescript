# KSeF number

## API

- `validateKsefNumber(ksefNumber)`

## Przyklad

```ts
import { validateKsefNumber } from "ksef-client-typescript";

const result = validateKsefNumber("KSEFNUMER...");
console.log(result.isValid);
```
